# PostgreSQL connection string
# For local dev: install Postgres locally OR use a free Neon/Supabase DB
# For Vercel: use Vercel Postgres or any external Postgres provider
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
